<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firdaus.tictactoegame">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_toys_black_24dp
"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Single_Player" />
        <activity android:name=".Single_player_3by3" />
        <activity android:name=".Interface" />
        <activity android:name=".Single_player_5by5" />
        <activity android:name=".Interface2" />
        <activity android:name=".Multiple_Player_5_by_5"></activity>
    </application>

</manifest>